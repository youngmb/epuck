 #epuck

`epuck` is a Python library for controlling and interacting with e-puck robots. It provides easy-to-use functions for various robot tasks like movement, sensor reading, and more.

## Installation

```bash
pip install epuck